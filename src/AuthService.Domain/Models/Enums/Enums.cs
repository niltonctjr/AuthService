namespace AuthService.Domain.Models.Enums
{
    public enum StateGeneric
    {
        Inactive = 0,
        Active = 1,        
    }

    public enum StatusDto
    {
        Invalid = -1,
        Error = 0,
        Success = 1,
    }
}